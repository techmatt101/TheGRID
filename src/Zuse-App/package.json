{
  "name": "Zuse-App",
  "version": "1.0.0",
  "description": "Developer Interface to the GRID",
  "author": "Matthew Kemp",
  "main": "index.js",
  "scripts": {
    "install": "tsd reinstall -s -o && npm run build",
    "build": "tsc app.ts --outDir build/ --target ES5 --module commonjs --sourceMap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "asciify": "^1.3.5",
    "boom": "^2.7.1",
    "config": "^1.12.0",
    "good": "^5.1.2",
    "good-console": "^4.1.0",
    "hapi": "^8.4.0",
    "joi": "^5.1.0",
    "services": "file:..\\Services-Lib"
  }
}
